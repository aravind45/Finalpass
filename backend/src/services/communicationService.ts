export interface CommRequest {\n  to: string;\n  from: string;\n  content: string;\n  estateId: string;\n}\n\nexport class CommunicationService {\n  static async sendFax(req: CommRequest): Promise<string> {\n    console.log(\Sending Fax via SRFax to \...\);\n    // TODO: Integrate SRFax API\n    return 'FAX-' + Math.random().toString(36).substr(2, 9);\n  }\n\n  static async sendCertifiedMail(req: CommRequest): Promise<string> {\n    console.log(\Sending Certified Mail via Lob to \...\);\n    // TODO: Integrate Lob API\n    return 'LOB-' + Math.random().toString(36).substr(2, 9);\n  }\n}
